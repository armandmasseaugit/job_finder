azure_credentials:
  account_name: ${oc.env:AZURE_STORAGE_ACCOUNT_NAME}
  account_key: ${oc.env:AZURE_STORAGE_ACCOUNT_KEY}

# DEPRECATED: AWS credentials disabled in favor of Azure
# aws_credentials:
#   key: ${oc.env:AWS_ACCESS_KEY_ID}
#   secret: ${oc.env:AWS_SECRET_ACCESS_KEY}
#   client_kwargs:
#     region_name: ${oc.env:AWS_REGION}

sender_email_password: ${oc.env:SENDER_EMAIL_PASSWORD}
