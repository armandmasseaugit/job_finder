# Azure Storage Configuration (used in the backend and frontend)
AZURE_STORAGE_ACCOUNT_NAME=your_storage_account_name
AZURE_STORAGE_ACCOUNT_KEY=your_storage_account_key

# Email Configuration (used in send_email kedro pipeline)
SENDER_EMAIL_PASSWORD=your_email_password

# ChromaDB Configuration
# To use the local ChromaDB (development):
CHROMA_DB_PATH=./data/chroma

# To use ChromaDB remote (production Azure VM):
# CHROMA_HOST=your-azure-vm-ip-or-domain
# CHROMA_PORT=8000
# CHROMA_SSL=false